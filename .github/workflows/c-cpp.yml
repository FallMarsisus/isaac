name: C/C++ CI

on:
  push:
    branches: [ "Antov2(caCommenceAFaireBeaucoup)" ]
  pull_request:
    branches: [ "Antov2(caCommenceAFaireBeaucoup)" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make
